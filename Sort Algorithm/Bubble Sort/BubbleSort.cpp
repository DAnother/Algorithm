#include "BubbleSort.h"

template<typename T>
void BubbleSort(T& data, int size){
    for(int i = 0; i < size-1; i++){
        for(int j = 0; j < size-1-i; j++){
            if(data[i] < data[j]){
                
            }
        }
    }
}
